[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "nexus-memory-system"
version = "1.0.0"
description = "A comprehensive, cross-agent memory management platform"
readme = "README.md"
license = {text = "MIT"}
authors = [
    {name = "scooter-lacroix", email = "scooter.lacroix@example.com"}
]
maintainers = [
    {name = "scooter-lacroix", email = "scooter.lacroix@example.com"}
]
classifiers = [
    "Development Status :: 5 - Production/Stable",
    "Intended Audience :: Developers",
    "Operating System :: OS Independent",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.9",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3.13",
    "Topic :: Scientific/Engineering :: Artificial Intelligence",
    "Topic :: Software Development :: Libraries :: Python Modules",
    "Topic :: Database",
    "Topic :: Internet :: WWW/HTTP :: HTTP Servers",
]
keywords = ["memory", "ai", "agents", "mcp", "context", "knowledge"]
requires-python = ">=3.9"

dependencies = [
    "fastmcp>=0.11.0",
    "fastapi>=0.115.0",
    "uvicorn[standard]>=0.32.0",
    "pydantic>=2.10.0",
    "pydantic-settings>=2.6.0",
    "loguru>=0.7.2",
    "sqlalchemy>=2.0.0",
    "alembic>=1.14.0",
    "aiofiles>=24.1.0",
    "jinja2>=3.1.0",
    "python-multipart>=0.0.17",
    "websockets>=14.0",
    "asyncpg>=0.30.0",
    "python-dotenv>=1.0.0",
    "click>=8.1.0",
    "rich>=13.9.0",
    "httpx>=0.28.0",
    "openai>=1.57.0",
    "tiktoken>=0.8.0",
    "numpy>=1.24.0",
    "scikit-learn>=1.5.0",
    "sentence-transformers>=3.3.0",
    "sqlite-vec>=0.1.1",
    "aiosqlite>=0.20.0",
]

[project.optional-dependencies]
dev = [
    "pytest>=8.3.0",
    "pytest-asyncio>=0.24.0",
    "pytest-cov>=6.0.0",
    "black>=24.10.0",
    "ruff>=0.8.0",
    "mypy>=1.14.0",
    "pre-commit>=4.0.0",
    "types-requests>=2.32.0",
    "types-setuptools>=75.6.0",
]
test = [
    "pytest>=8.3.0",
    "pytest-asyncio>=0.24.0",
    "pytest-cov>=6.0.0",
    "httpx>=0.28.0",
    "testcontainers>=4.9.0",
]
postgres = [
    "asyncpg>=0.30.0",
    "psycopg2-binary>=2.9.0",
]
embeddings = [
    "sentence-transformers>=3.3.0",
    "torch>=2.5.0",
    "transformers>=4.47.0",
]

[project.urls]
Homepage = "https://github.com/scooter-lacroix/nexus-memory-system"
Documentation = "https://github.com/scooter-lacroix/nexus-memory-system/docs"
Repository = "https://github.com/scooter-lacroix/nexus-memory-system.git"
"Bug Tracker" = "https://github.com/scooter-lacroix/nexus-memory-system/issues"
Changelog = "https://github.com/scooter-lacroix/nexus-memory-system/blob/main/CHANGELOG.md"

[project.scripts]
nexus = "nexus.cli:main"
nexus-serve = "nexus.main:run"
# nexus-ui = "nexus.web_ui.app:run_web_ui"  # TODO: implement web UI

[tool.setuptools.packages.find]
where = ["."]
include = ["nexus*"]

[tool.setuptools.package-data]
nexus = [
    "agents/scripts/**/*",
]

[tool.black]
line-length = 88
target-version = ['py39']
include = '\.pyi?$'
extend-exclude = '''
/(
  # directories
  \.eggs
  | \.git
  | \.hg
  | \.mypy_cache
  | \.tox
  | \.venv
  | build
  | dist
)/
'''

[tool.ruff]
line-length = 88
target-version = "py39"
select = [
    "E",  # pycodestyle errors
    "W",  # pycodestyle warnings
    "F",  # pyflakes
    "I",  # isort
    "B",  # flake8-bugbear
    "C4", # flake8-comprehensions
    "UP", # pyupgrade
]
ignore = [
    "E501",  # line too long, handled by black
    "B008",  # do not perform function calls in argument defaults
    "C901",  # too complex
]

[tool.ruff.per-file-ignores]
"__init__.py" = ["F401"]

[tool.mypy]
python_version = "3.9"
warn_return_any = true
warn_unused_configs = true
disallow_untyped_defs = true
disallow_incomplete_defs = true
check_untyped_defs = true
disallow_untyped_decorators = true
no_implicit_optional = true
warn_redundant_casts = true
warn_unused_ignores = true
warn_no_return = true
warn_unreachable = true
strict_equality = true

[tool.pytest.ini_options]
minversion = "8.0"
addopts = "-ra -q --strict-markers --strict-config"
testpaths = ["tests"]
asyncio_mode = "auto"
python_files = ["test_*.py", "*_test.py"]
python_classes = ["Test*"]
python_functions = ["test_*"]
markers = [
    "slow: marks tests as slow (deselect with '-m \"not slow\"')",
    "integration: marks tests as integration tests",
    "unit: marks tests as unit tests",
]

[tool.coverage.run]
source = ["nexus"]
omit = [
    "*/tests/*",
    "*/test_*.py",
    "*/__init__.py",
]

[tool.coverage.report]
exclude_lines = [
    "pragma: no cover",
    "def __repr__",
    "if self.debug:",
    "if settings.DEBUG",
    "raise AssertionError",
    "raise NotImplementedError",
    "if 0:",
    "if __name__ == .__main__.:",
    "class .*\\bProtocol\\):",
    "@(abc\\.)?abstractmethod",
]